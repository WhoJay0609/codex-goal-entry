# Trace Map

Use `goal-backend/scripts/validate_goal_trace.py` for `goal-run/v1` and
`goal-backend/scripts/read_legacy_trace.py` for legacy schema-versioned runs.
Legacy support is inspection only: executable replay and old script paths are
unsupported.
