# Metadata Refresh

Run `scripts/update_skill_inventory.py`, then
`scripts/generate_goal_stack_health.py`, then repository
`scripts/check_goal_stack.py`. Generated inventory reports installed and
unregistered skills; only a reviewed edit to the backend family registry grants
permission.
