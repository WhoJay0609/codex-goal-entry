# Goal Plan Builder

Build the smallest plan that can reach the durable outcome. Each work unit must
name one owner, dependencies, expected output, verification, integration point,
conflict owner, fallback, and wait/continue rule. Separate completed evidence
from readiness-only or proposed work.
