interface:
  display_name: "Goal Entry"
  short_description: "Route Goal requests by profile, lifecycle, and provider."
  default_prompt: "Use $goal-entry to resolve the Runtime Profile and lifecycle before creating or binding a goal."
