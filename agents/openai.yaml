interface:
  display_name: "Goal Entry"
  short_description: "Choose direct, Compound, or durable Goal execution."
  default_prompt: "Use $goal-entry as an explicitly invoked thin router. Choose direct only for read-only work, Compound Engineering for bounded mutations, or a Goal for durable project lifecycle needs; preserve any user-named professional skill."
